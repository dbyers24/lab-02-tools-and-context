'use strict'

const expect = require('expect');
const fp = require('../lib/fp.js');

describe('testing, Buhler', () => {

  describe('test', () => {
    it('should return ', () => {
      let result = fp.map([], n => n*2);
      expect(result).toEqual([]);
    });
  });
  describe
});
